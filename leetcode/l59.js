/**
 * @param {number} n
 * @return {number[][]}
 */
var generateMatrix = function(n) {
    let ret = [];
    // ret[n / 2 - 1][n / 2]
    for(let i = 0; i < n;i++) {
        for(let j = 0;j < n;j++) {

        }
    }
    ret.push()
    
    return ret;
};